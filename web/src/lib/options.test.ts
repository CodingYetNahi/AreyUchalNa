import{describe,expect,it}from'vitest';import{duplicateOptions,normaliseOptions,validateOptions}from'./options';describe('options',()=>{it('trims and removes blanks',()=>expect(normaliseOptions([' tea ','','  ','coffee'])).toEqual(['tea','coffee']));it('finds case-insensitive duplicates',()=>expect(duplicateOptions(['Tea',' tea ','COFFEE'])).toEqual(['tea']));it('requires two choices',()=>expect(validateOptions(['one'])).toContain('Add at least two choices.'))});
